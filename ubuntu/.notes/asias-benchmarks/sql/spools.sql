CREATE TABLE IF NOT EXISTS MODE_1_landing (
    EVENT_COUNT NUMBER,
    FLIGHT_COUNT NUMBER,
    FLIGHT_DATE VARCHAR2(11 BYTE),
    GROUP VARCHAR2(10 BYTE)
);

CREATE TABLE IF NOT EXISTS MODE_2_TAKEOFF (
    EVENT_COUNT NUMBER,
    FLIGHT_COUNT NUMBER,
    FLIGHT_DATE VARCHAR2(11 BYTE),
    GROUP VARCHAR2(10 BYTE)
);

CREATE TABLE IF NOT EXISTS MODE_2_LANDING (
    EVENT_COUNT NUMBER,
    FLIGHT_COUNT NUMBER,
    FLIGHT_DATE VARCHAR2(11 BYTE),
    GROUP VARCHAR2(10 BYTE)
);

CREATE TABLE IF NOT EXISTS EGPWS_LANDING (
    EVENT_COUNT NUMBER,
    FLIGHT_COUNT NUMBER,
    FLIGHT_DATE VARCHAR2(11 BYTE)
);

CREATE TABLE IF NOT EXISTS EGPWS_TAKEOFF (
    EVENT_COUNT NUMBER,
    FLIGHT_COUNT NUMBER,
    FLIGHT_DATE VARCHAR2(11 BYTE),
    GROUP VARCHAR2(10 BYTE)
);

CREATE TABLE IF NOT EXISTS AIRPORT_RANK (
    AIRPORT_ID_NAME VARCHAR2(10 BYTE),
    LANDING_OR_TAKEOFF VARCHAR2(50 BYTE),
    YEAR_QUARTER VARCHAR2(50 BYTE),
    EVENT
);

CREATE TABLE IF NOT EXISTS G_AIRPORT_RANK (
    AIRPORT_ID_NAME VARCHAR2(10 BYTE),
    LANDING_OR_TAKEOFF VARCHAR2(50 BYTE),
    YEAR_QUARTER VARCHAR2(
);
